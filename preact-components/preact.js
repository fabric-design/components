import * as React from 'react';
import * as ReactDOM from 'react-dom';
export const createElement = React.createElement || React.h;
export const Component = React.Component;
export const render = ReactDOM.render;